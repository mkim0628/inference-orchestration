# 사이클 요약 — 2026-05-28

## Activity: A+B (조합)
## 아이디어: HexAGenT DAG 워크플로우 스케줄러 (A-1) + PegaFlow RDMA 라우터 (A-2) + PegaFlow+Irminsul 분산 비연속 캐시 (B-1) + A+B 통합 파이프라인 (Cross-1)

## 핵심 결과
- Report ①: HexAGenT DAG 스케줄러(A) + PegaFlow+Irminsul 분산 비연속 캐시(B) + A+B Cross-1 파이프라인 구현 — 51개 테스트(44 단위 + 7 통합) 100% 통과, 1734개 이전 테스트 회귀 0건, 루프 1회차 Pass
- Report ②: vLLM 0.21.0 이식 Pass — HexAGeTWorkflowSchedulerMixin(A), PegaFlowIrminsulDistributedKVCacheManagerMixin(B), PegaFlowRDMASegmentAttentionHook(B) 세 패치 오류 없이 임포트; 스케줄링 오버헤드 p50 0.204ms; 4단계 히트율 합 1.0; issubclass Scheduler/KVCacheManager 양쪽 Pass; 루프 1회차 Pass (vLLM 0.21.0)

## 목표 지표 변화
| 지표 | 이전 최선 | 오늘 | 변화 |
|------|---------|------|------|
| Inference Throughput | +50.0% (2026-05-20 독립 실측) | null (Mock 환경) | N/A — Mock 환경 |
| KV Memory Reduction | −50.0% (2026-05-27) | 정상 추적 (memory_bytes() 검증) | 실측값 없음 — 구조 검증 Pass |
| Non-Contiguous Hit Rate | 100% (2026-05-27 독립) | 4단계 분산 히트율 합 1.0 검증 완료; 공유 프리픽스 기준 vLLM +100%p (0%→100%) | 구조적 히트율 향상 확인 |
| Effective Context Length | 2.0× (2026-05-27) | 구조적 지원 (PegaFlow 오프로딩) | N/A — 실측값 없음 |
| Compression Accuracy Delta | 0.00% (2026-05-27) | N/A (Activity C 미포함) | N/A |
| Scheduling Overhead | 2.6 µs/req (2026-05-27) | **0.204ms p50** (50 태스크 20 사이클); 1000 요청 단일 사이클 3.43ms | +0.204ms (이전 µs 수준 대비 증가; 그러나 목표 5ms 대비 4.1% — Pass) |

## 다음 사이클 제언
1. **RDMA 멀티 노드 실측 환경 구성**: `pegaflow_peer_nodes.yaml`에 실제 노드 등록 또는 멀티 프로세스 Mock으로 `rdma_remote_hit_rate` 30% 목표 실측 검증. RDMA 경로 코드는 완전히 준비됨.
2. **실제 LLM 추론 연동으로 throughput/TTFT 실측**: `src/engine/runner.py`와 HexAGenT DAG 파이프라인 연결로 `tokens/sec`, TTFT p50/p99 실측. A+B 통합의 실 처리량 효과 정량화. Activity A SLO 달성률(95%, 99%) 측정 포함.
3. **Activity C 통합으로 KV Memory −30% 목표 달성**: A+B 기반 위에 KV 캐시 압축(C)을 추가 — PegaFlow 오프로딩 경로에 INT8/FP8 양자화 적용이 자연스러운 확장점. `_try_evict_to_fit` 방어 코드(`if defer_id in self._deferred_kv` 가드) 보강도 병행.
